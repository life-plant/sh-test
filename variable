# 定义变量，使用变量
a_1="01234"
echo "变量a_1:$a_1"
# 英文字母 _ 数字，数字不能开头，不能是关键字

# 字符长度
echo "字符长度:${#a_1}"

#单引号只能输出字符串，双引号可以输出变量

# 只读属性
readonly a_1

# 截取字符
echo "截取字符a_1（1——3）:${a_1:1:3}"

# 查找字符位置
echo "字符位置:`expr index ${a_1} 3`"


# 数组
arr=('1' '2' '3')
echo "数组0:${arr[0]}"
echo "所有数组:${arr[@]}"

